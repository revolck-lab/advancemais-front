import React from 'react'
import styles from './loader-archives.module.css'

const LoaderArchives: React.FC = () => {
  return <span className={styles.loader}></span>
}

export default LoaderArchives
